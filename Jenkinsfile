pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Commands for building your app, e.g., Maven, Gradle, npm, etc.
            }
        }
        stage('Test') {
            steps {
                echo 'Testing...'
                // Commands for testing your app, e.g., unit tests
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Commands for deploying your app
            }
        }
    }
}
